#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(){
    float base, potencia, valor;
    int exp;
    string = "Bom"
    
    if(hora<12){
    	printf("Bom dia");
	}

    printf("Digite um valor: ");
    scanf("%f", &valor);

    printf("A resposta é: %f \n", sqrt(valor)); // sqrt raiz quadrada;

    system("pause");
	return 0;
}
